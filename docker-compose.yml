services:
  pawtrace-backend:
    build: ./pawtrace-backend
    ports:
      - "5001:5001"
    env_file:
      - ./pawtrace-backend/.env
    environment:
      - CORS_ORIGINS=http://localhost:5173,http://localhost:3000
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:5001/status')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 90s

  pawtrace-web:
    build: ./pawtrace-web
    ports:
      - "5173:5173"
    environment:
      - VITE_BACKEND_URL=http://pawtrace-backend:5001
    depends_on:
      pawtrace-backend:
        condition: service_healthy
